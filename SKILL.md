---
name: auto-period-switch-check
description: "Safely inspect and, only after explicit confirmation, switch period-based IDs in online CMS configurations driven by one or more spreadsheet sheets."
---

# 自动切期与配置检查

用于“在线表格驱动的 CMS 配置切期”任务：每期 ID 会变化，工作表可能不止一个，配置后台链接可能是单元格超链接而非显示文本。

## 不可违反的安全边界

- 默认只读；首次接到任务先读取表格、识别字段、生成待执行清单，等待用户确认。
- 任何线上保存、提交、发布、删除或批量替换都必须在动作即将发生前再次取得用户明确确认。
- 不猜测旧期/新期。优先使用用户指定的列；否则根据期号列排序和任务日期提出候选映射，并要求确认。
- 每个任务绑定：工作表、表格行号、旧ID、新ID、配置链接。禁止跨行取值。
- 旧ID匹配数为 0 时停止该行；大于 1 时必须列出全部位置并等待人工确认，确认后才可按明确范围批量替换。
- 页面结构变化、保存按钮缺失、登录失效、页面报错、保存后无法复查时停止。
- 使用浏览器当前登录状态；不得索要、记录或写入账号密码、Cookie、Token。
- 某行失败只暂停该行，不自动跳过异常或错误处理下一行。

## 执行模式

1. **检查模式（Level 1）**：只打开页面，读取可见字段/编辑器字段；报告旧ID数量、新ID数量、登录和页面结构状态。不修改。
2. **预览模式（Level 2）**：仅在旧ID唯一匹配时，展示字段位置、修改前内容、按当前行新ID生成的修改后内容。不保存。
3. **执行模式（Level 3）**：用户确认后修改已预览的位置；允许对同一页面或明确选定的多行执行批量替换，但必须先展示完整批次清单、逐行旧ID/新ID映射和匹配位置。保存/提交后立即重新读取页面，确认旧ID消失、新ID存在、页面可访问且保存结果可确认。

详细步骤、状态和日志字段见 [references/runbook.md](references/runbook.md)。

## 表格处理

- 支持多个工作表：先列出工作表名称，让用户指定范围或按名称/模式筛选；不要默认处理全部工作表。
- 支持 `.xlsx`、`.csv` 或在线表格导出的文件。在线表格优先通过浏览器只读导出副本。
- 自动识别候选旧ID列、新ID列和配置链接来源，但必须展示映射并等待确认。
- 配置链接来源依次支持：链接列的超链接目标、CMS名称列的超链接目标、纯文本 URL。读取超链接目标，不要只读取显示文本。
- 忽略仅由格式产生的空白尾行；保留原始表格行号。
- 多行 ID 单元格按多个值处理，进入“需人工确认”，不得拆分后批量替换。
- 批量替换必须显式指定范围（工作表/行号/页面/字段）；禁止用一个新ID替换多个不同任务的旧ID。

## 浏览器操作

- 使用当前浏览器登录状态和页面可见/可访问 DOM；优先稳定的 label、placeholder、name、id 或 data 属性。
- 对 iframe、富文本或动态编辑器，读取内部 input、textarea、select、contenteditable 等字段；记录实际字段定位信息。
- 不使用坐标猜测进行修改；找不到唯一目标时停止。
- 先读取并记录修改前快照，再执行单字段修改；不要点击保存以外的副作用按钮。
- 批量执行前必须完成 Level 2 预览并获得一次明确的批次确认；任一行发生异常时暂停该行及后续批次，不自动跳过。

## 输出

生成待执行清单、Level 1/2/3 结果和逐行审计日志。日志至少包含时间、工作表、行号、旧ID、新ID、配置链接、模式、状态、错误原因、修改前结果、修改后检查结果和截图/快照引用。
